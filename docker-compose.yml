version: '3'

services:
  sun-panel-helper:
    image: madrays/sun-panel-helper:v1.7.1
    ports:
      - "33002:80"
    volumes:
      - /path/to/sunpanel/conf/custom:/app/deploy
    restart: unless-stopped